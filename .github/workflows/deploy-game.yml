name: Deploy Biome World

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/deploy-game.yml'
      - 'scripts/**'
      - 'publish/**'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download build (combat + winlose)
        run: |
          mkdir -p publish
          curl -fsSL -o /tmp/game.zip "https://tmpfiles.org/dl/1787662883.1797d0ba249f8f84/wBw7GmAJBWPc/game_fix_all.zip"
          unzip -o /tmp/game.zip -d /tmp/game_extract
          HTML=$(find /tmp/game_extract -name '*.html' | head -1)
          cp "$HTML" publish/index.html
          if [ -s bg-music.mp3 ]; then cp bg-music.mp3 publish/bg-music.mp3
          else curl -fsSL -o publish/bg-music.mp3 "https://raw.githubusercontent.com/t17067042-code/biome-world-tg/main/bg-music.mp3" || true
          fi
          touch publish/.nojekyll
          ls -la publish/
          wc -c publish/index.html
          grep -c 'updateRTSUnits\|endGame\|hitRTSUnit\|gamePaused' publish/index.html || true

      - name: Publish to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: publish
          force_orphan: true
          enable_jekyll: false
